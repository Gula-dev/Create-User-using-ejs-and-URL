<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white min-h-screen p-5">
    <div class="container mx-auto max-w-7xl">
        <a class="text-blue-500" href="/"> ← Back to home</a>
        <h1 class="text-3xl mt-8 tracking-tighter mb-5">All Users</h1>

        <div class="users grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 xl:grid-cols-4 gap-6">
            <% if(users.length>0){%>
                <% users .forEach(function(user){ %>
                    <div
                        class="user max-w-xs w-full p-6 bg-zinc-800 rounded-lg shadow-lg hover:scale-105 transition-transform">
                        <div class="w-full aspect-[4/5] rounded-lg bg-zinc-700 overflow-hidden">

                            <img class="w-full h-full object-cover aspect-[4/5]" src="<%=user.img_url %>" alt="">

                        </div>
                        <h3 class="text-xl mt-3 tracking-tighter">
                            <%= user.name%>
                        </h3>
                        <h5 class="text-zinc-500">
                            <%= user.email%>
                        </h5>
                        <div class="flex gap-3 mt-6">
                            <a class="text-zinc-200" href="/edit/<%= user._id%>">Edit User</a>
                            <a class="text-red-500" href="/delete/<%= user._id%>">Delete User</a>
                        </div>
                    </div>
                    <% })%>
                        <% }else{%>
                         <h3 class="text-zinc-500">No User Yet</h3>
                            <% } %>



        </div>
    </div>

    <script src="/public/javascripts/"></script>
</body>

</html>